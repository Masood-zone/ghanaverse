import { requireUserPage } from "@/lib/auth/access";
import { getPrisma } from "@/lib/prisma";
export default async function ProfilesPage() { const { user } = await requireUserPage("/profiles"); const profiles = await getPrisma().viewerProfile.findMany({ where: { userId: user.id }, orderBy: { createdAt: "asc" } }); return <section className="mx-auto max-w-5xl px-4 py-14 md:px-6"><p className="text-xs font-bold uppercase tracking-[.16em] text-primary">Viewer profiles</p><h1 className="mt-3 text-4xl font-extrabold">Who&apos;s watching?</h1><p className="mt-3 text-muted-foreground">Choose a profile for My List and viewing progress. Profile editing arrives with the next account-management increment.</p><div className="mt-10 grid grid-cols-2 gap-5 sm:grid-cols-4">{profiles.map((profile) => <div key={profile.id} className="rounded-xl border bg-card p-5 text-center shadow-sm"><div className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-[linear-gradient(135deg,#d97706,#15803d)] text-2xl font-extrabold text-white">{profile.name.slice(0, 1).toUpperCase()}</div><h2 className="mt-4 font-bold">{profile.name}</h2><p className="mt-1 text-xs text-muted-foreground">Active viewer profile</p></div>)}</div></section>; }
