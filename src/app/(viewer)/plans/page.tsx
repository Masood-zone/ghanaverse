import { FoundationPage } from "@/components/layout/foundation-page";
export default function PlansPage() { return <FoundationPage eyebrow="Plans" title="Membership plans are not active yet." description="Billing and subscription processing are intentionally deferred beyond the foundation phase." />; }
