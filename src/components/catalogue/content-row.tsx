import { ContentCard } from "@/components/catalogue/content-card";
export function ContentRow({ title, items }: { title: string; items: Parameters<typeof ContentCard>[0]["item"][] }) { if (!items.length) return null; return <section className="mt-12"><div className="mb-5 flex items-end justify-between"><h2 className="text-2xl font-extrabold tracking-tight">{title}</h2><span className="text-sm font-semibold text-primary">Explore</span></div><div className="grid grid-cols-2 gap-4 sm:grid-cols-3 lg:grid-cols-5 xl:grid-cols-6">{items.map((item) => <ContentCard key={item.slug} item={item} />)}</div></section>; }
