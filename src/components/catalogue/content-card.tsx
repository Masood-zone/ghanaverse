import Link from "next/link";
type Item = { slug: string; title: string; shortDescription: string; contentType: string; releaseYear: number | null; runtimeMinutes: number | null; genres: { genre: { name: string } }[] };
export function ContentCard({ item }: { item: Item }) { return <Link href={`/title/${item.slug}`} className="group block min-w-0"><div className="relative aspect-[2/3] overflow-hidden rounded-xl bg-[linear-gradient(145deg,#6b3d12,#d97706_45%,#15803d)] p-4 shadow-sm transition group-hover:-translate-y-1 group-hover:shadow-lg"><div className="absolute inset-0 bg-[radial-gradient(circle_at_80%_10%,rgba(255,255,255,.35),transparent_30%)]" /><div className="relative flex h-full flex-col justify-end"><p className="text-[10px] font-bold uppercase tracking-[.16em] text-white/80">{item.contentType.replaceAll("_", " ")}</p><h3 className="mt-2 text-xl font-extrabold leading-tight text-white">{item.title}</h3><p className="mt-2 line-clamp-2 text-xs leading-5 text-white/80">{item.shortDescription}</p></div></div><div className="mt-3"><h3 className="font-bold">{item.title}</h3><p className="mt-1 text-xs text-muted-foreground">{[item.releaseYear, item.genres[0]?.genre.name].filter(Boolean).join(" · ")}</p></div></Link>; }
